name: "autonomy_stack_future_prediction"
description: "World model for future frame prediction"

world_model:
  type: "encoder_decoder"
  latent_dim: 32
  input_channels: 3
  output_channels: 3

training:
  learning_rate: 1e-4
  batch_size: 32
  epochs: 20
  loss: "mse"

device: "cpu"
