name: "autonomy_stack_offline_rl"
description: "Offline RL (TD3+BC) training"

algorithm: "td3_bc"

td3:
  actor_lr: 1e-3
  critic_lr: 1e-3
  gamma: 0.99
  tau: 0.005

bc:
  alpha: 0.5
  loss: "mse"

training:
  batch_size: 64
  epochs: 50
  replay_buffer_size: 100000

device: "cpu"
